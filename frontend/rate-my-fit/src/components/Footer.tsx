import { Typography } from '@mui/material';

const Footer = () => (
    <Typography sx={{
        color: 'custom.gray', textAlign: 'center'
    }}>
        ©️ 2025, FitRater
    </Typography>
);

export default Footer;