import { Fit } from '../types/fit';
import { Item } from '../types/item';
import { Card } from '@mui/material';

const FitCard = (FitData: Fit, ItemsData: Item[]) => {
    return (
        <Card>
            
        </Card>
    );
};

export default FitCard;